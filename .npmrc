# Install devDependencies even when the shell has NODE_ENV=production (e.g. after
# `npm run start` in the same terminal). Without Tailwind/PostCSS, `globals.css`
# fails to compile and every route returns 500.
include=dev
